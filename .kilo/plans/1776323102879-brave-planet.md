# 《末光咏叹》开发计划

> 基于 `feature_analysis.md` 需求分析，经讨论筛选后的实际开发优先级。

## 排除项（不做）

- ❌ 音效反馈（SFX/BGM）
- ❌ 战斗动画/粒子效果
- ❌ 成就系统
- ❌ 多角色/多职业
- ❌ 转生/轮回系统（Prestige）

---

## Phase 1：强化系统重构

**目标**：为金币建立核心回收循环，同时增加强化决策深度。

**当前问题**：
- 强化 100% 成功率，无风险无决策
- 强化仅是装备详情里的一个按钮，无独立 UI
- 金币中后期必然严重溢出（强化 + 50G 回血是唯二出口）

### 1.1 成功率机制

| 强化等级 | 成功率 | 计算公式 |
|----------|--------|----------|
| +0 → +1 | 95% | 基础成功率 |
| +1 → +2 | 88% | 95% - 7% |
| +2 → +3 | 81% | 95% - 14% |
| ... | ... | 95% - (当前等级 × 7%) |
| +9 → +10 | 32% | 95% - 63% → 被 min(30%) 截断为 32%... |
| +10+ | 30% | 硬下限，不再递减 |

公式：`successRate = Math.max(30, 95 - currentEnhanceLv * 7)` （单位：%）

- **失败惩罚**：仅损失金币，强化等级不变
- **成功效果**：与现有一致（主属性指数增长 + 副属性 +0.5%）

### 1.2 强化费用调整

当前公式：`Math.max(100, (rarityIdx + 1) * 300 * (enhanceLv + 1))`

调整为更激进的指数增长：

```javascript
let baseCost = (rarityIdx + 1) * 300;
let cost = Math.floor(baseCost * Math.pow(1.8, enhanceLv));
```

| 强化等级 | 普通装备 | 终焉装备 |
|----------|----------|----------|
| +1 | 540G | 5,940G |
| +5 | 4,307G | 47,374G |
| +8 | 25,059G | 275,649G |
| +10 | 81,117G | 892,287G |
| +12 | 262,819G | 2,891,009G |

结合成功率递减，+10 以上的期望消耗会非常高，形成强力金币回收。

### 1.3 强化 UI

从装备详情弹窗中剥离，在背包弹窗中新增"⚒️ 强化"按钮（位于手动分解按钮旁边），点击后切换到强化视图。

**入口位置**：背包弹窗内，与"手动分解"按钮并列，新增"⚒️ 强化锻造"按钮。点击后进入强化模式，背包列表切换为强化装备列表。

**布局设计**（复用背包弹窗的左右结构）：
```
┌─────────────────────────────────────────┐
│  ⚒️ 强化锻造台            [↩ 返回背包]    │
├──────────────────┬──────────────────────┤
│  左侧：装备列表   │  右侧：强化详情       │
│  排序：与终焉精炼  │                      │
│  一致（已装备按   │  装备名 +等级          │
│  槽位顺序顶置，   │  主属性：xxxx          │
│  背包中的在后）   │  副属性：xxxx          │
│                  │                      │
│  [装备中]武器 +3  │  成功率：72.0%         │
│  [装备中]头盔 +5  │  费用：12,500G         │
│  [背包]  法杖 +0  │                      │
│  [背包]  法袍 +2  │  [⚒️ 强化] 按钮        │
│  ...             │                      │
│                  │  连续强化 ×5 ×10 ×MAX │
│                  │  (批量强化直到成功/     │
│                  │   金币耗尽，显示摘要)   │
└──────────────────┴──────────────────────┘
```

**排序规则**（与终焉精炼一致，见 `ui.js:renderRefineList`）：
1. 已装备的装备按固定槽位顺序排列（weapon → head → chest → legs → ring → trinket）
2. 背包中的装备排在后面
3. 已装备的条目标记 `[装备中]`，背景色区分

**功能清单**：
- 背包弹窗内新增"⚒️ 强化锻造"按钮，与"手动分解"并列
- 点击后进入强化模式：左侧切换为强化装备列表，右侧为强化详情
- "↩ 返回背包"按钮可切回普通背包视图
- 强化按钮：单次强化
- 批量强化：×5 / ×10 / ×MAX（自动重复直到成功或金币耗尽，显示结果摘要）
- 强化动画/反馈：成功绿色闪光，失败红色闪光（纯 CSS）

### 1.4 涉及文件

- `engine.js`
  - `Player` 类：新增 `enhanceGear(gear)` 方法，内含成功率判定逻辑
  - 移除 `ui.js` 中的旧 `enhanceGear()` 方法
- `ui.js`
  - 新增 `openEnhanceView()` / `renderEnhanceList()` / `renderEnhanceDetail()` / `doEnhance()` / `batchEnhance()`
  - 在背包弹窗底部操作区新增"⚒️ 强化锻造"按钮（位于手动分解按钮旁）
  - 移除旧强化按钮相关代码（装备详情中的 `enhanceGear` 按钮）
- `index.html` — 无需新增独立 overlay（复用背包弹窗的 modal 结构）
- `data.js` — 无需改动（强化公式在 engine 中计算）

### 1.5 关键注意

- 成功率显示精确到小数点后一位（如 `72.0%`）
- 批量强化结果以摘要形式弹出（"尝试 5 次：成功 2 次，失败 3 次，共消耗 62,500G"）
- 终焉装备的强化费用系数 11x（rarityIdx=8），是核心金币回收大户
- 强化成功率受装备品质影响——高品质装备成功率不额外降低，但费用更高，间接增加消耗
- 存档兼容：`enhanceLv` 字段已存在，无需新增；成功率在运行时计算，不持久化

---

## Phase 2：DPS 面板 / 战斗统计

**目标**：让玩家能量化排轴优劣，验证 Build 有效性。

**当前问题**：有文字战斗日志，但无法直观得知秒伤、技能占比。

### 功能清单

| 统计项 | 说明 |
|--------|------|
| 实时 DPS | 最近 5 秒滑动窗口总伤害 / 5 |
| 技能伤害占比 | 各技能伤害占总伤害百分比，降序排列 |
| 暴击率 | 本次战斗实际暴击触发率 |
| 总治疗量 | 所有治疗技能总恢复量 |
| 护盾承伤 | 护盾吸收的总伤害量 |
| 战斗时长 | 从遇到怪物到击杀的用时 |

### 涉及文件

- `engine.js` — 新增 `CombatStats` 类，嵌入 `CombatEngine`；在 `calcDmg()`、`processEffects()` 中埋点记录
- `ui.js` — 新增可折叠 DPS 面板渲染
- `index.html` — 新增 DPS 面板容器 DOM

### 数据结构

```javascript
class CombatStats {
    constructor() { this.reset(); }
    reset() {
        this.startTime = 0;
        this.totalDmg = 0;
        this.totalHeal = 0;
        this.shieldAbsorbed = 0;
        this.critCount = 0;
        this.totalHits = 0;
        this.skillDmgMap = {};  // { skillId: { name, totalDmg, hits, crits } }
        this.recentDmgLog = []; // { timestamp, dmg } 滑动窗口
    }
    recordHit(skillId, skillName, dmg, isCrit) { ... }
    recordHeal(val) { ... }
    recordShieldAbsorb(val) { ... }
    getDPS() { ... }
    getSkillBreakdown() { ... }
    getCritRate() { ... }
}
```

### 关键注意

- `recentDmgLog` 滑动窗口只保留最近 5 秒数据
- 每场战斗开始（`spawnMob()`）时 `reset()`
- DPS 面板通过 `EBus.emit('ui_dps_update', stats)` 每 500ms 刷新一次，避免高频渲染
- 击杀怪物时输出本次战斗摘要到战斗日志

---

## Phase 3：动态技能描述

**目标**：技能说明动态显示实际数值，排轴时有据可依。

**当前问题**：`SKILLS_DB` 中 `desc` 全部为静态字符串，如 `"造成220%伤害"`。

### 实现方案

将 `desc` 字段改为模板格式，UI 渲染时根据玩家当前面板动态替换：

```javascript
// 旧: desc: '[GCD] 造成220%伤害，消耗12法力，冷却8秒。'
// 新: desc: '[GCD] 造成{dmgMult}%攻击力的伤害（约{dmg}），消耗{cost}法力，冷却{cd}秒。'
```

### 涉及文件

- `data.js` — 全部 42 个技能的 `desc` 字段改为模板格式
- `ui.js` — 新增 `renderDynamicDesc(skill, player)` 函数，在技能池/技能轴/Tooltip 处调用

### 模板语法

| 占位符 | 替换逻辑 |
|--------|----------|
| `{dmg}` | `formatNumber(Math.floor(atk * dmgMult * (1 + versa/100)))` |
| `{dmgMult}` | `(skill.dmgMult * 100).toFixed(0)` |
| `{cost}` | `skill.cost` |
| `{cd}` | `(skill.cd / 1000).toFixed(1)` |
| `{heal}` | `formatNumber(Math.floor(max(atk,int) * healMult))` |
| `{dur}` | `(effect.dur / 1000).toFixed(0)` |

### 关键注意

- 保持 `desc` 可读性，模板标记简洁
- 多效果技能（如真言术·慰同时造成伤害和回蓝）按顺序用 `{dmg}` `{heal}` 等依次替换
- `desc` 仅影响技能池/技能轴的 Tooltip 显示，不影响战斗日志输出

---

## Phase 4：进阶拾取过滤

**目标**：允许玩家按词条+品质组合规则精确控制拾取/分解。

**当前问题**：只有一个下拉框选择丢弃品质阈值，无法细粒度过滤。

### 功能清单

| 过滤规则 | 说明 | 示例 |
|----------|------|------|
| 品质门槛 | 低于此品质直接分解 | ≥ 传说 |
| 词条过滤 | 只保留带指定词条的装备 | 戒指必须带 crit |
| 词条数值门槛 | 词条值低于阈值则分解 | crit ≥ 8% |
| 部位独立规则 | 不同部位可设不同规则 | 武器全留，防具只要 haste |

### 涉及文件

- `data.js` — 新增默认过滤规则模板
- `engine.js` — `Player.lootItem()` 中替换原有 `autoThreshold` 逻辑，改为调用 `LootFilter.shouldKeep(gear)`
- `ui.js` — 新增拾取过滤设置面板
- `index.html` — 新增过滤设置面板 DOM

### 数据结构

```javascript
{
    slot: 'ring',              // 部位，null 表示全局
    minRarity: 5,              // 最低品质索引（传说）
    requiredAffixes: ['crit'], // 必须包含的词条
    minAffixValues: {           // 词条最低数值
        crit: 8
    }
}
```

### 关键注意

- 规则存入 `Storage`，持久化
- 优先级：部位规则 > 全局规则 > 旧版品质下拉框（兼容保留）
- 过滤面板提供"快捷预设"按钮（如"只留终焉"、"只留高暴击首饰"）

---

## Phase 5（后调）：商店/黑市系统

金币回收已由强化概率机制承担，商店定位调整为补充性消费，待其他系统稳定后再设计具体商品。

---

## Phase 6（后调）：离线收益计算

**当前问题**：`requestAnimationFrame` 在页面隐藏后暂停或 deltaTime 跳跃。

### 实现概要

1. 监听 `document.visibilitychange`，隐藏时记录 `lastOnlineTimestamp` 到 Storage
2. 页面可见时计算离线时长，超过 30s 触发结算
3. 根据挂机前 DPS + 胜率估算，快速模拟 N 场战斗
4. 批量发放经验/金币/掉落，弹出离线收益报告
5. 离线时长硬上限 8 小时，超时按 8 小时计算

### 涉及文件

- `engine.js` — 新增 `OfflineCalculator` 类、`CombatEngine` 初始化检测
- `ui.js` — 离线收益弹窗
- `index.html` — 弹窗 DOM

---

## Phase 7（后调）：敌方技能与 AI

**当前问题**：`Monster` 类纯粹是数值沙袋，Boss 只是血量和攻击力的倍率提升。

### 实现概要

1. `data.js` 新增 `ENEMY_SKILLS_DB`（眩晕/反伤/护盾/回血/AOE）
2. `Monster` 类扩展 `skills[]`，Boss 随机分配 1-3 个敌方技能
3. `CombatEngine.processMobAI()` 增加敌方技能执行逻辑
4. 新增玩家状态 `stunned`、怪物状态 `reflecting`
5. 让排轴从"打木桩"升级为"对策编排"

### 涉及文件

- `data.js` — `ENEMY_SKILLS_DB`、`BOSS_TEMPLATES`
- `engine.js` — 扩展 `Monster`、`processMobAI()`、新增 `processEnemySkills()`
- `ui.js` — 怪物 Buff 栏、玩家眩晕状态视觉反馈

---

## Phase 3.5：跨平台自适应 UI（CSS 模块化重构 + 响应式）

**目标**：将 CSS 从 `index.html` 的 `<style>` 标签拆分为模块化文件，并实现 PC/移动端自动适配。

### 文件结构

```
css/
├── main.css          # 入口文件：@import 其他文件 + 响应式规则
├── base.css          # 公共样式：变量、重置、按钮、颜色、日志等（不随设备变化）
├── layout-pc.css     # PC 端布局：三列宽度、网格、间距
└── layout-mobile.css # 移动端覆盖：单列、全宽、弹窗适配、Tab 导航
```

### 各文件内容划分

**`base.css`**（设备无关）：
- `:root` CSS 变量
- `*` 全局重置 + `body` 基础样式
- 滚动条样式
- `button` 通用样式
- `.bar-container` / `.bar-fill` / `.bar-text` / `.hp-bar` / `.mp-bar` / `.exp-bar`
- `.stats-grid` / `.stat-item`
- `.equip-slot` / `.equip-name` / `.equip-stats` / `.enhance-tag`
- `.map-node` 系列样式
- `.monster-area` / `.monster-name` / `.buff-bar` / `.buff-icon`
- `.log-area` / `.log-item` / `.log-*` 颜色类
- `.tabs` / `.tab` / `.tab.active`
- `.skill-btn` / `.s-name` / `.s-desc` / `.cd-mask`
- `.seq-item` / `.seq-actions` / `.opener-tag`
- `.inv-item` / `.details-box`
- `.toast-*` 系列样式
- `.q0` ~ `.q8` 品质颜色
- `.enhance-flash-*` / `@keyframes flash-*`

**`layout-pc.css`**（PC 端 ≥769px）：
- `.wrapper` 三栏 flex
- `.col-left` 25% / `.col-mid` 45% / `.col-right` 30%
- `.panel` / `.panel-title`
- `.equip-grid` 双列
- `.combat-controls`
- `.seq-editor` / `.skill-pool` / `.curr-sequence` / `.pool-grid` 双列
- `#modal-overlay` / `.modal` 600px / `.inv-grid` / `#modal-content-area`
- `#refine-overlay` / `.refine-modal` 700px / `.refine-left` 35% / `.refine-right` 65%
- `#enhance-overlay`（复用 `.refine-modal`）
- `#gm-panel`

**`layout-mobile.css`**（移动端 ≤768px 覆盖）：
- `.wrapper` → `flex-direction: column`
- `.col-left` / `.col-mid` / `.col-right` → `width: 100%; display: none`
- `.col-left.active` / `.col-mid.active` / `.col-right.active` → `display: flex`
- `.mobile-tabs` → `display: flex`（PC 端 `display: none`）
- `.combat-controls` → `flex-wrap: wrap; gap: 6px`
- `.map-radar` → `font-size: 11px; flex-wrap: wrap`
- `.equip-grid` → `grid-template-columns: 1fr 1fr`（不变，但间距缩小）
- `.pool-grid` → `grid-template-columns: 1fr`
- `.modal` → `width: 95%; max-height: 90vh`
- `.refine-modal` → `flex-direction: column; width: 95%; max-height: 90vh`
- `.refine-left` / `.refine-right` → `width: 100%; border-right: none; padding: 0`
- `#modal-content-area` → `max-height: 50vh`
- `button` → `min-height: 36px; min-width: 36px`（触控友好）
- `.monster-area` → `height: 120px`
- 字体微调

### 手机 Tab 导航

在 `.wrapper` 之前添加：

```html
<nav class="mobile-tabs">
    <button class="mobile-tab active" data-panel="col-left">👤 角色</button>
    <button class="mobile-tab" data-panel="col-mid">⚔️ 战斗</button>
    <button class="mobile-tab" data-panel="col-right">🧠 技能</button>
</nav>
```

PC 端隐藏（`display: none`），手机端显示（`display: flex`）。
JS：点击 Tab 切换对应面板的 `.active` 类。

### `index.html` 修改

1. 添加 `<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">`
2. 删除 `<style>` 全部内容，替换为 `<link rel="stylesheet" href="css/main.css">`
3. 在 `.wrapper` 之前添加 `<nav class="mobile-tabs">` HTML

### `js/main.js` 修改

添加手机端 Tab 切换事件监听：

```javascript
document.querySelectorAll('.mobile-tab').forEach(btn => {
    btn.addEventListener('click', () => {
        document.querySelectorAll('.mobile-tab').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        let panel = btn.dataset.panel;
        ['col-left', 'col-mid', 'col-right'].forEach(id => {
            document.querySelector('.' + id).classList.toggle('active', id === panel);
        });
    });
});
```

### `main.css` 结构

```css
@import url('base.css');
@import url('layout-pc.css');

@media (max-width: 768px) {
    @import url('layout-mobile.css');
}
```

### 关键注意

- 桌面端（>768px）体验零改动，所有移动端覆盖仅通过 `layout-mobile.css` 触发
- 不引入任何外部 CSS 框架，纯原生实现
- 弹窗内的左右布局（精炼/强化）在手机端自动转为上下布局
- 所有原有颜色值、尺寸值、动画效果不改变，只进行拆分和归类

---

## Bug 修复清单（Phase 4 代码审查）

### BUG-1：`openLootFilter()` 每次打开都重新绑定 preset 事件

**文件**：`ui.js:1363-1373`

每次调用 `openLootFilter()` 都对 `.lf-preset-btn` 和 `#btn-lf-save` 赋值 `onclick`。虽用赋值而非 `addEventListener` 不累积，但不整洁。

**修复**：将绑定移到 `constructor` 或 `bindEvents()` 中，`openLootFilter()` 只负责显示和 render。

---

### BUG-2（关键）：`batchSalvage()` 仍用旧版品质下拉框，未接入 LootFilter

**文件**：`ui.js:584-607`

`batchSalvage()` 从 `#ui-salvage` 读取阈值只按 `rarityIdx` 过滤。而 `lootItem()` 已用 `LootFilter.shouldKeep()`。两者标准不一致，导致背包中堆积"不符合进阶过滤但符合旧品质阈值"的装备，手动批量分解无法清理。

**修复**：修改 `batchSalvage()` 改用 `!LootFilter.shouldKeep(gear)` 判断是否应分解，保留下拉框作为兜底。

---

### BUG-3：全局规则品质下拉框文案歧义

**文件**：`ui.js:1403`

全局规则的 `<select>` 首选项为"继承全局/使用下拉框"。但全局规则本身就是最顶层，"继承全局"语义不通。

**修复**：`_renderLootFilterSection` 中根据 prefix 区分文案——全局用"不设品质门槛（回退到下拉框）"，部位用"继承全局规则"。

---

### BUG-4：`minRarity === -1` 时下拉框首选项未显式 `selected`

**文件**：`ui.js:1403-1406`

`value="-1"` 的 `<option>` 缺少 `${rule.minRarity === -1 ? 'selected' : ''}`。浏览器默认选中首项故功能正确，但代码不严谨。

**修复**：为首 option 添加条件 selected。

---

### 修复优先级

| 优先级 | Bug | 影响 |
|--------|-----|------|
| **高** | BUG-2：batchSalvage 未接入 LootFilter | 背包堆积无法清理 |
| **中** | BUG-3：全局下拉框文案歧义 | 用户困惑 |
| **中** | BUG-4：minRarity=-1 未显式 selected | 代码严谨性 |
| **低** | BUG-1：事件重复绑定 | 整洁性 |

---

## Bug 修复实施方案

### BUG-1：事件重复绑定 → 移到 constructor

**文件**：`ui.js`

将 `openLootFilter()` 中的 preset 按钮和 save 按钮绑定移到 `constructor` 末尾。`openLootFilter()` 只保留 `display='flex'` 和 `_renderLootFilter()` 调用。

**constructor 新增**（在 `this.currentEnhanceList = [];` 之后）：
```javascript
// 拾取过滤按钮绑定（只绑定一次）
this._bindLootFilterEvents();
```

**新增方法 `_bindLootFilterEvents()`**：
```javascript
_bindLootFilterEvents() {
    document.querySelectorAll('.lf-preset-btn').forEach(btn => {
        btn.onclick = () => {
            let preset = btn.dataset.preset;
            if (preset === 'reset') LootFilter.resetRules();
            else LootFilter.applyPreset(preset);
            this._renderLootFilter();
        };
    });
    document.getElementById('btn-lf-save').onclick = () => {
        this._saveLootFilter();
        document.getElementById('loot-filter-overlay').style.display = 'none';
        EBus.emit('log', '🔍 拾取过滤规则已保存。', 'sys');
    };
}
```

**`openLootFilter()` 简化为**：
```javascript
openLootFilter() {
    document.getElementById('loot-filter-overlay').style.display = 'flex';
    this._renderLootFilter();
}
```

---

### BUG-2（关键）：batchSalvage 接入 LootFilter

**文件**：`ui.js` — `batchSalvage()` 方法

将原来只按 `rarityIdx` 过滤的逻辑改为 `!LootFilter.shouldKeep(gear)` + 兜底下拉框。

**替换**：
```javascript
batchSalvage() {
    let salvageCount = 0;
    let hasAdvancedRules = Object.keys(LootFilter.loadRules().global).some(
        k => (k === 'minRarity' && LootFilter.loadRules().global[k] > -1) ||
             (k === 'requiredAffixes' && LootFilter.loadRules().global[k].length > 0) ||
             (k === 'minAffixValues' && Object.keys(LootFilter.loadRules().global[k]).length > 0)
    ) || Object.keys(LootFilter.loadRules().slots).length > 0;

    for (let i = this.player.inventory.length - 1; i >= 0; i--) {
        let g = this.player.inventory[i];
        if (g.locked || g.pinned) continue;
        let shouldSalvage = hasAdvancedRules ? !LootFilter.shouldKeep(g) : (g.rarityIdx <= parseInt(document.getElementById('ui-salvage').value));
        if (shouldSalvage) {
            this.player.salvageGear(g);
            this.player.inventory.splice(i, 1);
            salvageCount++;
        }
    }
    if (salvageCount > 0) {
        EBus.emit('ui_bars_update');
        EBus.emit('log', `♻️ 批量清理了 ${salvageCount} 件装备。`, 'sys');
        this._renderBackpack();
    } else {
        alert("没有符合条件且未锁定的装备可分解。");
    }
}
```

**逻辑说明**：如果存在任何进阶过滤规则（全局非空 或 有部位规则），用 `LootFilter.shouldKeep()`；否则回退到旧版下拉框逻辑。这样"不自动分解"时不会误删。

---

### BUG-3：全局规则下拉框文案区分

**文件**：`ui.js` — `_renderLootFilterSection()`

将第 1403 行的硬编码文案改为根据 prefix 动态生成：

```javascript
let placeholderText = prefix === 'global' ? '不设品质门槛（回退到下拉框）' : '继承全局规则';
let rarityOptions = `<option value="-1" ${rule.minRarity === -1 ? 'selected' : ''}>${placeholderText}</option>`;
```

---

### BUG-4：minRarity=-1 显式 selected

已在 BUG-3 的修复中一并解决——`<option value="-1" ${rule.minRarity === -1 ? 'selected' : ''}>`。

---

### 修改文件汇总

| 文件 | 修改内容 |
|------|----------|
| `js/ui.js` | constructor 新增 `_bindLootFilterEvents()` 调用；新增 `_bindLootFilterEvents()` 方法；`openLootFilter()` 精简；`batchSalvage()` 接入 LootFilter；`_renderLootFilterSection()` 修复文案和 selected |

---

## Phase 4.5：新增5个终焉技能（s43-s47）

### 技能规格

| ID | 名称 | 类型 | 冷却 | 消耗 | 效果 | 描述 |
|----|------|------|------|------|------|------|
| s43 | 时间回溯 | ogcd | 180s | 100MP | 重置所有非终焉技能冷却 + 5秒施法速度+50% | `[终焉][oGCD] 扭曲时间，重置所有常规技能冷却并大幅提升施法速度。` |
| s44 | 灵魂献祭 | gcd | 180s | 50%当前HP | 消耗HP×20倍伤害 + 10秒增伤30% | `[终焉][GCD] 献祭生命精华，造成巨额伤害并进入强化状态。` |
| s45 | 星辰坠落 | gcd | 180s | 120MP | 8秒每秒400%伤害 + 免疫 + 无法使用其他技能 | `[终焉][GCD] 召唤星辰坠落，持续轰击敌人的同时自身进入无敌状态。` |
| s46 | 命运轮转 | ogcd | 180s | 0 | HP≤30%时：恢复100%HP+MP + 15秒增伤50%；否则不触发 | `[终焉][oGCD] 绝境中逆转命运，瞬间恢复全部状态并大幅强化。` |
| s47 | 虚空化身 | buff | 180s | 80MP | 15秒DoT伤害+100%、DoT生效间隔-50% | `[终焉][Buff] 化身虚空，接下来15秒内所有持续伤害的伤害提高100%，生效间隔缩短50%。` |

**解锁等级**：待确认（候选人：86/89/92/96/98 填充现有终焉间隔，或 85/90/95/99/100 与现有效果平行同级）

---

### 引擎新增机制

#### 1. 新增 effect handler（追加到 `effectHandlers` 映射表）

**`cd_reset`**（s43 时间回溯）：
- 遍历 `engine.skills`，将 `isFinale === false` 的技能 `currentCd` 置零
- 同时追加 `{type:'buff', stat:'haste', val:50, dur:5000}` 实现5秒急速+50%
- 日志：`重置所有常规技能冷却！`

**`hp_sacrifice`**（s44 灵魂献祭）：
- `hpCost = Math.floor(engine.player.hp * e.costPct)`
- 扣除 `engine.player.hp -= hpCost`
- 计算伤害：`dmg = hpCost * e.dmgMult`（乘以 versa/vuln/dmg_up_pct 但不roll暴击）
- 对怪物造成伤害
- 追加 `{type:'buff', stat:'dmg_up_pct', val:30, dur:10000}` 实现10秒增伤30%
- 日志：`献祭 {hpCost} HP，造成 {dmg} 伤害`

**`channel_immune`**（s45 星辰坠落）：
- 追加 `{type:'channel_immune', dur:8000}` 到 `player.buffs`
- 追加 `{type:'dot', dur:8000, dps:4.0, tickTimer:0}` 到 `mob.buffs`
- 日志：`进入星辰坠落状态，持续8秒！`

**`cond_full_heal`**（s46 命运轮转）：
- 恢复 `engine.player.hp = engine.player.getMaxHp()`
- 恢复 `engine.player.mp = engine.player.getMaxMp()`
- 追加 `{type:'buff', stat:'dmg_up_pct', val:50, dur:15000}` 实现15秒增伤50%
- 注意：HP>30%的条件检查在 `executeSkill()` 前置判断中处理，此处只执行满足条件后的效果
- 日志：`命运逆转！生命与法力完全恢复！`

**`dot_enhance`**（s47 虚空化身）：
- 追加 `{type:'dot_enhance', dur:15000}` 到 `player.buffs`
- 日志：`化身虚空，持续伤害大幅增强！`

#### 2. `attemptExecution()` 修改（s45 蓄力锁定）

在方法最开头、任何技能选择逻辑之前，添加蓄力检测：

```javascript
// 蓄力状态检测：蓄力期间不可施放任何技能
if (this.player.buffs.some(b => b.type === 'channel_immune')) {
    return;
}
```

#### 3. `executeSkill()` 修改（s46 条件技能前置检查）

在 `this.player.mp -= s.cost;` 之前，添加条件检测：

```javascript
// 条件技能前置检查：不满足条件则不执行、不消耗、不触发CD
if (s.conditionMaxHPPct !== undefined) {
    let hpPct = (this.player.hp / this.player.getMaxHp()) * 100;
    if (hpPct > s.conditionMaxHPPct) {
        return; // 条件不满足，技能保留
    }
}
```

#### 4. `processMobAI()` 修改（s45 免疫伤害）

在怪物攻击伤害计算之前，添加免疫检测：

```javascript
// 免疫状态检测：免疫期间不受任何伤害
if (this.player.buffs.some(b => b.type === 'channel_immune')) {
    this.mob.attackTimer = 0; // 重置怪物攻击计时器
    return;
}
```

#### 5. `processEffects()` 中 DoT tick 修改（s47 DoT增强）

在现有 DoT 逻辑 `if (eff.type === 'dot' && eff.tickTimer >= 1000)` 处，替换为动态间隔检测：

```javascript
// DoT 增益检测：虚空化身期间 tick 间隔减半
let dotTickInterval = 1000;
let dotEnhance = this.player.buffs.some(b => b.type === 'dot_enhance');
if (dotEnhance) dotTickInterval = 500;

if (eff.type === 'dot' && eff.tickTimer >= dotTickInterval) {
    eff.tickTimer -= dotTickInterval;
    let effectiveDps = dotEnhance ? eff.dps * 2 : eff.dps;
    let res = this.calcDmg(effectiveDps, vulnMult);
    this.mob.hp -= res.val;
    this.log(`☠️ [流血跳跃] 造成 ${formatNumber(res.val)} 点伤害`, 'sys');
}
```

---

### data.js 技能定义

```javascript
{ id: 's43', name: '时间回溯', reqLv: 待定, type: 'ogcd', cd: 180000, cost: 100, dmgMult: 0,
  effects: [{type:'cd_reset'}, {type:'buff', stat:'haste', val:50, dur:5000}],
  priority: 19, desc:'[终焉][oGCD] 扭曲时间，重置所有常规技能冷却并大幅提升施法速度。消耗{cost}法力，冷却{cd}秒。' },

{ id: 's44', name: '灵魂献祭', reqLv: 待定, type: 'gcd', cd: 180000, cost: 0, dmgMult: 0,
  effects: [{type:'hp_sacrifice', costPct:0.5, dmgMult:20}, {type:'buff', stat:'dmg_up_pct', val:30, dur:10000}],
  priority: 16, desc:'[终焉][GCD] 献祭生命精华，造成巨额伤害并进入强化状态。冷却{cd}秒。' },

{ id: 's45', name: '星辰坠落', reqLv: 待定, type: 'gcd', cd: 180000, cost: 120, dmgMult: 0,
  effects: [{type:'channel_immune', dur:8000}, {type:'dot', dur:8000, dps:4.0}],
  priority: 17, desc:'[终焉][GCD] 召唤星辰坠落，持续轰击敌人的同时自身进入无敌状态。消耗{cost}法力，冷却{cd}秒。' },

{ id: 's46', name: '命运轮转', reqLv: 待定, type: 'ogcd', cd: 180000, cost: 0, dmgMult: 0,
  conditionMaxHPPct: 30,
  effects: [{type:'cond_full_heal'}, {type:'buff', stat:'dmg_up_pct', val:50, dur:15000}],
  priority: 20, desc:'[终焉][oGCD] 绝境中逆转命运，瞬间恢复全部状态并大幅强化。冷却{cd}秒。' },

{ id: 's47', name: '虚空化身', reqLv: 待定, type: 'buff', cd: 180000, cost: 80, dmgMult: 0,
  effects: [{type:'dot_enhance', dur:15000}],
  priority: 18, desc:'[终焉][Buff] 化身虚空，接下来15秒内所有持续伤害的伤害提高100%，生效间隔缩短50%。消耗{cost}法力，冷却{cd}秒。' },
```

**关键设计决策**：
- s44 的 `cost: 0`（MP消耗为0，HP消耗在 `hp_sacrifice` handler 中处理）
- s44 的 `dmgMult: 0`（不使用常规伤害计算，伤害在 handler 内独立计算）
- s45 的 DoT 直接追加到 `mob.buffs`，由现有 DoT tick 机制驱动
- s46 新增 `conditionMaxHPPct` 字段，在 `executeSkill()` 前置检查
- s43 中的 `{type:'buff', stat:'haste', val:50}` 复用现有 buff 机制，绕过装备急速上限

---

### ui.js renderDynamicDesc 新增占位符

以上5个技能的 desc 模板使用的占位符均为已有类型（`{cost}`、`{cd}`、`{dur}`），无需新增占位符。

---

### 修改文件汇总

| 文件 | 修改内容 |
|------|----------|
| `js/data.js` | 新增 s43-s47 五个技能定义 |
| `js/engine.js` | 新增 5 个 effect handler；`attemptExecution()` 添加蓄力检测；`executeSkill()` 添加条件技能前置检查；`processMobAI()` 添加免疫检测；`processEffects()` DoT tick 添加 dot_enhance 增益 |

---

### 待确认

- [x] s43-s47 解锁等级（已确认并实施，ID已按reqLv重编号）

---

## Phase 5：装备槽位扩展（6→8槽）

> 护腿→腿部+足部，戒坠→吊坠+戒指，遗物不变

### 新槽位体系

| 旧ID | 旧名称 | 新ID | 新名称 | 类型 |
|------|--------|------|--------|------|
| weapon | 武器 | weapon | 武器 | 武器 |
| head | 头盔 | head | 头部防具 | 防具 |
| chest | 胸甲 | chest | 身体防具 | 防具 |
| legs | 护腿 | legs | 腿部防具 | 防具 |
| - | - | **feet** | **足部防具** | **防具**（新增，沿用legs词库） |
| ring | 戒坠 | **pendant** | **吊坠** | **首饰**（从ring拆出） |
| - | - | **ring** | **戒指** | **首饰**（沿用ring词库） |
| trinket | 遗物 | trinket | 遗物 | 首饰 |

### 分类判定函数（替代所有硬编码）

新增全局工具函数，统一三分类判定：

```js
// engine.js 顶部导出
export function isAccessory(slot) { return slot === 'ring' || slot === 'pendant' || slot === 'trinket'; }
export function isArmor(slot) { return slot === 'head' || slot === 'chest' || slot === 'legs' || slot === 'feet'; }
export function isWeapon(slot) { return slot === 'weapon'; }
```

**替换规则**：所有 `slot === 'ring' || slot === 'trinket'` → `isAccessory(slot)`；所有 `slot === 'head' || slot === 'chest' || slot === 'legs'` → `isArmor(slot)`；所有 `slot === 'weapon'` → `isWeapon(slot)`。

---

### 一、data.js 修改

#### 1.1 SLOTS 数组
```js
export const SLOTS = ['weapon', 'head', 'chest', 'legs', 'feet', 'pendant', 'ring', 'trinket'];
```

#### 1.2 SLOT_NAMES 映射
```js
export const SLOT_NAMES = {
  weapon: '武器', head: '头部', chest: '胸甲', legs: '腿甲', feet: '战靴',
  pendant: '吊坠', ring: '戒指', trinket: '遗物'
};
```

#### 1.3 SLOT_BASE_NAMES 词库

**旧词库分析**：
- `legs`: ['护腿', '法裤', '轻靴', '长裤', '布靴', '灵鞋'] — 混合了腿部和足部名称
- `ring`: ['魔戒', '指环', '印戒', '暗环', '秘戒'] — 纯戒指，无需迁移
- `trinket`: ['挂坠', '护符', '圣徽', '魂匣', '宝珠', '颈饰'] — 混合了吊坠和遗物

**新词库**：
```js
export const SLOT_BASE_NAMES = {
  weapon: ['法杖', '魔杖', '咒刃', '魂弓', '法典', '灵刃', '权杖', '秘典'],
  head: ['法冠', '巫帽', '兜帽', '光环', '灵冠', '面具', '额饰'],
  chest: ['法袍', '轻甲', '长袍', '皮衣', '圣衫', '布衣', '符文衣'],
  legs: ['护腿', '法裤', '长裤', '胫甲', '腿铠'],               // 腿部专用（移除靴类）
  feet: ['战靴', '法靴', '轻靴', '布靴', '灵鞋', '铁履'],       // 足部（从旧legs移出靴类+新增）
  pendant: ['挂坠', '护符', '圣徽', '颈饰', '魂链'],             // 吊坠（从旧trinket移出+新增）
  ring: ['魔戒', '指环', '印戒', '暗环', '秘戒'],                // 戒指（沿用原ring不变）
  trinket: ['魂匣', '宝珠', '秘符', '圣器', '核心']              // 遗物（纯遗物风格）
};
```

**自检**：删除0行，新增约10行（feet+pendant词库，SLOTS扩展2项，SLOT_NAMES扩展2项）

---

### 二、engine.js 修改

#### 2.0 新增分类工具函数（文件顶部，class之前）
```js
export function isAccessory(slot) { return slot === 'ring' || slot === 'pendant' || slot === 'trinket'; }
export function isArmor(slot) { return slot === 'head' || slot === 'chest' || slot === 'legs' || slot === 'feet'; }
export function isWeapon(slot) { return slot === 'weapon'; }
```

#### 2.1 GearGenerator.generate — 宝珠孔位按品质初始化（替换固定3孔）
```js
// 旧：orbs: [null, null, null],
// 新：按品质决定孔位数
let orbSlots = 0;
if (rIdx === 8) orbSlots = 3;          // 终焉3孔
else if (rIdx >= 5) orbSlots = 2;      // 传说/神话/圣物2孔
else if (rIdx >= 2) orbSlots = 1;      // 精良/卓越/史诗1孔
// 破损/普通0孔
gear.orbs = new Array(orbSlots).fill(null);
```

#### 2.2 GearGenerator.generate — 防具主属性判断
```js
// 旧：else if (slot === 'head' || slot === 'chest' || slot === 'legs')
// 新：
else if (isArmor(slot))
```

#### 2.3 GearGenerator.generate — 首饰标识
```js
// 旧：let isAccessory = (slot === 'ring' || slot === 'trinket');
// 新：
let isAccessory = isAccessory(slot);  // 注意变量名冲突！改用局部变量名 accCheck
let accCheck = isAccessory(slot);
```
**注意**：局部变量 `isAccessory` 与导出函数同名会冲突，局部变量统一改名为 `accCheck`。

#### 2.4 GearGenerator.getAffixCap — 首饰判断
```js
// 旧：if (slot === 'ring' || slot === 'trinket')
// 新：if (isAccessory(slot))
```

#### 2.5 Player.load() — 终焉图鉴默认值改为动态生成
```js
// 旧：硬编码 { weapon: false, head: false, chest: false, legs: false, ring: false, trinket: false }
// 新：动态从SLOTS生成，并兼容旧存档
let defaultCollection = {};
SLOTS.forEach(s => defaultCollection[s] = false);
this.finaleCollection = d.finaleCollection || defaultCollection;
// 旧存档兼容：补全缺失的新槽位字段
SLOTS.forEach(s => { if (this.finaleCollection[s] === undefined) this.finaleCollection[s] = false; });
```

#### 2.6 recalcStats — 暴击分类
```js
// 旧：let isArmor = (slot === 'head' || slot === 'chest' || slot === 'legs');
//     let isAccessory = (slot === 'ring' || slot === 'trinket');
// 新：
let armorCheck = isArmor(slot);
let accCheck = isAccessory(slot);
// 后续判断也相应替换
```

#### 2.7 recalcStats — 暴击分头上限调整
4防具+2首饰+1武器共7件有暴击，防具暴击上限需调整：
```js
// 旧：Math.min(50, weaponCrit) + Math.min(30, armorCrit) + Math.min(20, accessoryCrit)
// 新：防具4件×7.5%=30 → 不变；首饰3件×6.67%=20 → 不变（pendant加入后首饰变3件）
this.stats.crit += Math.min(50, weaponCrit) + Math.min(30, armorCrit) + Math.min(20, accessoryCrit);
```
**结论**：上限数值不变（防具30、首饰20仍然合理），只改分类函数。

#### 2.8 enhanceGear — 全部硬编码替换
- `gear.slot === 'ring' || gear.slot === 'trinket'` → `isAccessory(gear.slot)`
- `gear.slot === 'weapon'` → `isWeapon(gear.slot)`
- 防具else分支自动覆盖feet

#### 2.9 refineAffix — 同上替换

#### 2.10 baseScore 3处重复代码 — 同上替换isAccessory判断

#### 2.11 LootFilter 预设 — high_crit_accessory
```js
// 旧：rules.slots.ring = ...; rules.slots.trinket = ...;
// 新：加入 pendant
rules.slots.pendant = { minRarity: 5, requiredAffixes: ['crit'], minAffixValues: { crit: 5 } };
rules.slots.ring = { ... };
rules.slots.trinket = { ... };
```

#### 2.12 旧存档兼容 — orbs 数组长度调整
在 `Player.load()` 或 `recalcStats` 首次遍历装备时，检测 `gear.orbs` 长度与品质规则不符时截断或补null：
```js
// 在 Player.load() 末尾添加
this._migrateGearOrbs();
```
新增方法：
```js
_migrateGearOrbs() {
  let allGears = [...Object.values(this.equips).filter(Boolean), ...this.inventory];
  allGears.forEach(gear => {
    if (!gear.orbs) gear.orbs = [];
    let expected = 0;
    if (gear.rarityIdx === 8) expected = 3;
    else if (gear.rarityIdx >= 5) expected = 2;
    else if (gear.rarityIdx >= 2) expected = 1;
    while (gear.orbs.length < expected) gear.orbs.push(null);
    if (gear.orbs.length > expected) gear.orbs = gear.orbs.slice(0, expected);
  });
}
```

#### 2.13 图鉴注释更新
- `getMaxHp` 注释 "集齐6件" → "集齐8件"
- `recalcStats` 注释同上

#### 2.14 旧存档兼容 — equips 中旧 ring 键处理

**用户决策**：旧存档中 `gear.slot='ring'` 的装备（魔戒/指环等）**保留在ring不动**，对应新体系的"戒指"。语义完全匹配，无需迁移。pendant（吊坠）槽位初始为空，玩家通过获取新装备填充。

**无需任何 equips.ring → equips.pendant 迁移代码。**

**自检**：engine.js 修改约40行（替换硬编码+新增工具函数+迁移逻辑），删除0行注释，无未列出功能。

---

### 三、ui.js 修改

#### 3.1 import 新增 isAccessory/isArmor/isWeapon
```js
import { ..., isAccessory, isArmor, isWeapon } from './engine.js';
```

#### 3.2 openGearDetail — 槽位判断替换
```js
// 旧：let isAccessory = (gear.slot === 'ring' || gear.slot === 'trinket');
// 新：let accCheck = isAccessory(gear.slot);
```

#### 3.3 宝珠孔位渲染 — 按gear.orbs实际长度动态循环
```js
// 旧：if (!gear.orbs) gear.orbs = [null, null, null]; for (let i = 0; i < 3; i++)
// 新：
if (!gear.orbs) gear.orbs = [];
let orbLen = gear.orbs.length;
if (orbLen === 0) {
  statsHtml += `<span style="color:#555; font-size:11px;">该装备无宝珠孔位</span>`;
} else {
  for (let i = 0; i < orbLen; i++) { ... }
}
```

#### 3.4 renderEnhanceDetail — 同3.2替换

#### 3.5 旧装备orbs兼容 — ui.js Line 531
已由 engine.js `_migrateGearOrbs()` 在load时处理，ui.js 只需移除 `gear.orbs = [null, null, null]` 硬编码回退。

**自检**：ui.js 修改约10行，删除1行硬编码回退，无未列出功能。

---

### 四、main.js 修改

#### 4.1 GM genUltimate — SLOTS遍历自动适配
已有 `SLOTS.forEach(slotName => ...)` 代码，无需改动。但需确认 import 的 SLOTS 已更新。

**自检**：0行改动。

---

### 五、实施顺序与验证

1. **data.js**：改 SLOTS / SLOT_NAMES / SLOT_BASE_NAMES
2. **engine.js**：新增3个工具函数 → 替换所有硬编码判断 → 宝珠孔位按品质初始化 → 旧存档迁移 → 图鉴注释
3. **ui.js**：import工具函数 → 替换硬编码 → 宝珠孔位动态渲染
4. **验证**：语法检查3个js文件

### 六、自检总结

| 文件 | 新增行 | 修改行 | 删除行 | 删除原有注释？ | 新增未列出功能？ |
|------|--------|--------|--------|---------------|-----------------|
| data.js | ~10 | 3 | 0 | 否 | 否 |
| engine.js | ~25 | ~30 | 0 | 否 | 否 |
| ui.js | ~5 | ~8 | 1 | 否 | 否 |
| main.js | 0 | 0 | 0 | 否 | 否 |
